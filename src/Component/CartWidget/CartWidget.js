import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faCartPlus } from "@fortawesome/free-solid-svg-icons";

const CartWidget = () => {
  return <FontAwesomeIcon size="3x" icon={faCartPlus} color="white" />;
};

export default CartWidget;
